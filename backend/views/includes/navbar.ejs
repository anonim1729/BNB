<%if(!currUser){%>
  <nav class="navbar bg-body-tertiary" style="line-height: 4rem">
    <div class="container-fluid">
      <div class="mx-auto">Your farm at one stop location <a class="text-body-secondary"
          href="/listings">Green-Minds</a>. Please Login to continue</div>
    </div>
  </nav>
  <%}%>
    <nav class="navbar navbar-expand-sm bg-body-light sticky-top border-bottom border-top mb-3"
      style="background-color: white;line-height: 3rem;">
      <div class="container-fluid">
        <a class="navbar-brand col-1" href="/">
          <i class="fa-solid fa-leaf fs-1 mx-5" style="color: #63E6BE;"></i>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
          aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup" style="background-color: white;">
          <div class="navbar-nav">
            <a class="nav-link" href="/">Home</a>
            <a class="nav-link" href="/workbench">Work Bench</a>
            <a class="nav-link" href="/inventory">Inventory</a>
          </div>



          <div class="navbar-nav ms-auto">
            <a class="nav-link btn btn-light rounded-pill" href="/farm">Your farm</a>&nbsp;

            <% if(!currUser){ %>
              <button class="btn btn-outline-success rounded-pill"
                onclick="location.href = '/signUp';">Sign-Up</button>&nbsp;
              <button class="btn btn-outline-success rounded-pill"
                onclick="location.href = '/login';">Log-In</button>&nbsp;
              <% } %>
                <% if(currUser){ %>
                  <a class="nav-link" href="/logout">Log-Out</a>&nbsp;
                  <% } %>
          </div>
        </div>
      </div>
    </nav>